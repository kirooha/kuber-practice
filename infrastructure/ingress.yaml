apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kuber-practice-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kuber-practice-app-service
                port:
                  number: 8080